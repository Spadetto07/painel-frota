{
  "name": "painel-frota",
  "version": "1.0.0",
  "private": true,
  "description": "Painel de disponibilidade da frota, servido por Cloudflare Worker e protegido por Cloudflare Access",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute painel-frota-db --remote --file=./migrations/0001_inicial.sql"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
